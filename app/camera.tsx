import { Text, View } from "react-native";

export default function Camera() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text style={{ fontSize: 24 }}>Camera Screen</Text>
    </View>
  );
}
